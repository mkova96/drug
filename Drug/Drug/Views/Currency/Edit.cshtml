@model DrugData.Models.ViewModels.EditCurrencyViewModel

<form asp-action="Update" asp-route-id="@Model.Currency.CurrencyId" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    @if (@ViewData["Success"] != null)
    {
        <div class="alert alert-success">Valuta spremljena</div>
    }
    <h2>Uredi</h2>
    <div class="form-group">
        <label asp-for="Currency.CurrencyName">Naziv </label>
        <input asp-for="Currency.CurrencyName" class="form-control" />
    </div>
    <div class="text-center">
        <button type="submit" class="btn btn-primary">Spremi</button>
        <a asp-action="Index">
            <button type="button" class="btn btn-secondary">Odustani</button>
        </a>
    </div>

</form>
